package com.omni.backend.populator;

import com.omni.backend.dto.ActionData;
import com.omni.backend.model.EntityModel;

public class EntityEntryPopulator implements Populator<EntityModel, ActionData> {

    public ActionData populate() {
        return null;
    }
}